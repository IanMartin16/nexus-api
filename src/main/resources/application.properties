spring.application.name=nexus-api

# Perfil por defecto (local)
spring.profiles.active=local

# Flyway y JPA (común)
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

nexus.web-secret=${NEXUS_WEB_SECRET:}
openai.base-url=https://api.openai.com/v1
openai.api-key=${OPENAI_API_KEY:}
openai.model=gpt-4.1
openai.max-output-tokens=700
openai.timeout-ms=20000

cryptolink.base-url=https://cryptolink.mx
cryptolink.timeout-ms=6000